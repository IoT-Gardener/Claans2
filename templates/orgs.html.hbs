<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registered Orgs</title>
</head>
<body>
<section id="hello">
    <h1>Orgs</h1>
    New Orgs
    <ul>
        {{#each orgs}}
            <li>Id: {{ this.org_id }}</li>
            <li>Name: {{ this.org_name }}</li>
        {{else}}
            <p> No orgs yet</p>
        {{/each}}
    </ul>
</section>
</body>
</html>
